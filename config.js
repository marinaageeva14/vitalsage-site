// Generated at deploy time by .github/workflows/deploy-pages.yml from the
// PSI_API_KEY repository secret. This committed copy intentionally ships an
// empty key so the source never contains the real one.
window.VITALSAGE_CONFIG = { psiKey: '' };
